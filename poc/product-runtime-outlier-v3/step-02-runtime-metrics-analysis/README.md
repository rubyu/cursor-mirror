# Step 02: Runtime Metrics Analysis

Use `..\scripts\analyze_product_runtime_outlier_package.ps1` to summarize scheduler, controller, overlay, and optional coalescing-related telemetry fields.

Expected outputs:

- `metrics.json`
- `report.md`
