# Step 03: Coalescing Field Review

Review analyzer output for CSV columns whose names contain `coalesc`.

If no such columns are present, record that the package predates coalescing telemetry or does not emit it for the tested path.
