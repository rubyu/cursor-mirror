# Notes

- Calibrator runs are interactive Windows runs: they move the real cursor, enter full screen, block user mouse input, and stop on any key press.
- Commands in this step are intentionally explicit and output files are stored outside `poc/` by default.
- The first scoring grid should use the full generated Lab package. Shorter runs are smoke tests only.
- Existing root capture packages are not v22 scoring inputs.
